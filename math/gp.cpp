#include <iostream>
#include <cmath>
using namespace std;

int main() {
    int n;
    cin >> n;
    //in this problem we have three thing that are given in the problem 
    //that is first one is first term , common differnece and the last terms that is n
    double r, a;
    cin>>a>>r;
    cout<< a * (pow(r, n) - 1)) / (r - 1);
}    
    